{% extends 'base.html.twig' %}

{% block title %}Categories{% endblock %}

{% block body %}
<div class="container">
    <div class="row mt-4">
        <h1>Categories</h1>
    </div>
    <div class="row mt-3">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ category.Category }}</td>
            </tr>
        </tbody>
    </table>
    </div>
    <div class='row mt-3'>
    <a class="text-primary" href="{{ path('categories_index') }}">back to list</a>
    </div>
    <div class='row mt-3'>
    <a class="text-success" href="{{ path('categories_edit', {'id': category.id}) }}">edit</a>
    </div>
    <div class='row mt-3'>
    {{ include('categories/_delete_form.html.twig') }}
    </div>
{% endblock %}
