{% extends 'base.admin.html.twig' %}

{% block body %}
<div class="container">
    <div class="row mt-4">
        <div class="col-md-12">
            <a class="text-primary" href="{{ path('categories_index') }}">back to list</a>
        </div>
    </div>
    <div class="row mt-4">
        <div class="col-md-12">
            <h1>Edit Categories</h1>
        </div>
    </div>
    <div class="row mt-3">
        <div class="col-md-12">
                {{ include('categories/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
    </div>
    <div class="row mt-3">
        <div class="col-md-12">
                {{ include('categories/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
