<form method="post" action="{{ path('g_c_s_g_c_u_delete', {'id': g_c_s_g_c_u.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ g_c_s_g_c_u.id) }}">
    <button class="btn btn-warning">Delete</button>
</form>
