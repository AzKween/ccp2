{% extends 'base.admin.html.twig' %}

{% block body %}
<div class="container">
    <div class="row mt-4">
        <h1>GCSGCU</h1>
    </div>
    <div class="row mt-3">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ g_c_s_g_c_u.id }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ g_c_s_g_c_u.Text }}</td>
            </tr>
        </tbody>
    </table>
    </div>
        <div class='row mt-3'>
            <a class="text-primary" href="{{ path('g_c_s_g_c_u_index') }}">back to list</a>
        </div>
        <div class='row mt-3'>
            <a class="text-success" href="{{ path('g_c_s_g_c_u_edit', {'id': g_c_s_g_c_u.id}) }}">edit</a>
        </div>
        <div class='row mt-3'>
            {{ include('g_c_s_g_c_u/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
