{% extends 'base.admin.html.twig' %}

{% block body %}
<div class="container">
    <div class="row mt-4">
        <h1>Kinds</h1>
    </div>
    <div class="row mt-3">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ kind.id }}</td>
            </tr>
            <tr>
                <th>Kind</th>
                <td>{{ kind.Kind }}</td>
            </tr>
        </tbody>
    </table>
    </div>
        <div class='row mt-3'>
            <a class="text-primary" href="{{ path('kinds_index') }}">back to list</a>
        </div>
        <div class='row mt-3'>
            <a href="{{ path('kinds_edit', {'id': kind.id}) }}">edit</a>
        </div>
        <div class='row mt-3'>
            {{ include('kinds/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
